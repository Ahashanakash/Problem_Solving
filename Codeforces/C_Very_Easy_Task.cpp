#include<bits/stdc++.h>
using namespace std;

#define ll long long


void solve()
{
    ll n,x,y,sum=0,i=1;
    ll x=min(x,y);
    ll cnt=0;
    while (sum<n)
    {
        sum+=x;
    }
    
    
    
}
int main (){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    solve();
return 0;
}