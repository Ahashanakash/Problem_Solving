#include<bits/stdc++.h>
using namespace std;

int main (){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    long long t;
    cin>>t;
    while(t--)
    {
        long long p,c;
        cin>>p>>c;
        if (p>c)
        {
            cout<<"NO"<<endl;
        }
        else cout<<"YES"<<endl;
    }
return 0;
}